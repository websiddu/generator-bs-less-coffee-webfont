language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-bs-less-coffee-webfont' ]; then cd .. && eval "mv $currentfolder generator-bs-less-coffee-webfont" && cd generator-bs-less-coffee-webfont; fi
